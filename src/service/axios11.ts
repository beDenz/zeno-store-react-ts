import React from "react";
import axios from "axios";
/*
//console.log(process.env.REACT_APP_URL_DB);
const url: string = process.env.REACT_APP_URL_DB as string;

const getData = async () => {
  let tempArray: any = [];
  const data: any = await axios.get(`${url}/data.json`);
  const dataArray = Object.keys(data.data).map(key => {
    return { ...data.data[key] };
  });

  console.log(dataArray);
};
const setData = async () => {
  const note = { title: "some", id: 5 };
  await axios.post(`${url}/data.json`, note);
  //console.log([data.data);
};



/////////////////////
  const url: string = process.env.REACT_APP_URL_DB as string;

  const setDataInstall = async (item: any) => {
    await axios.post(`${url}/blogsList.json`, item);
  };

  blogPost.map((item: any) => setDataInstall(item));
////////////////////////
//getData();
//setData();
*/
